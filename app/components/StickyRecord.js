import React from 'react';
//import ReactDOM from 'react-dom';

import PropTypes from 'prop-types';

class StickyRecord extends React.Component {
  static propTypes = {
    colSpan: PropTypes.integer,
  }

  render() {
    return ;
  }
}

export default StickyRecord;
